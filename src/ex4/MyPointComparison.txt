ア,aという変数にMyPointクラスのインスタンスを宣言している。

イ,dという変数にインスタンス変数aを代入している。

ウ,aの位置は(6,8)に対してbの位置は(10,10)で宣言されているため

エ,aとcが1.3.にて同じ引数で宣言をされているため。

オ,d=aで一度宣言された後、aに変更が一度もないため

カ,aとbの比較演算子でfalseが出力されている。

キ,5行目と同じく異なる数値の引数で宣言されているインスタンス変数のため。

ク,aとcは同じ数値のインスタンス変数ではあるがメモリ上では別物で存在してい
るからだ。

ケ,dはaを初期値として代入しており、dの宣言以降a2変更が一度もないため同一のものであると言えるから
