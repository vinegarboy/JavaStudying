メソッドのシグネチャとはメソッド名、引数の型、引数の数の3つを組み合わせた総称である。基本的にはシグネチャが同じメソッドは同一のクラス内では宣言することができない。

次にオーバーライドとはサブクラスでスーパークラスの持つメソッドを上書きして宣言することである。これによりサブクラスインスタンスではスーパークラスのインスタンスとは異なる機能を実装することができる。
最後にオーバーロードとは同一クラス内で同名のメソッドではあるが引数の型や引数の数が異なるメソッドを宣言できる仕組みのことである。これを活用することで引数が異なる場合に異なる動作をするシステムを構築することができる。